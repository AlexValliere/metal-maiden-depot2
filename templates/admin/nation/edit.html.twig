{% extends 'admin/dashboard/base.html.twig' %}

{% block title %}Edit Nation{% endblock %}

{% block body %}
	<h1>Edit Nation</h1>

	{{ include('admin/nation/_form.html.twig', {'button_label': 'Update'}) }}

	<div class="d-inline-block mt-2 mb-4">
		<a href="{{ path('admin_nation_index') }}"><button class="btn d-inline-block">{{ 'label.back_to_list'|transchoice(0)|capitalize }}</button></a>
	</div>

	{{ include('admin/nation/_delete_form.html.twig') }}
{% endblock %}