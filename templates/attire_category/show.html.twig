{% extends 'base.html.twig' %}

{% block title %}AttireCategory{% endblock %}

{% block body %}
    <h1>AttireCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attire_category.id }}</td>
            </tr>
            <tr>
                <th>Abbreviation</th>
                <td>{{ attire_category.abbreviation }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ attire_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('attire_category_index') }}">back to list</a>

    <a href="{{ path('attire_category_edit', {'id': attire_category.id}) }}">edit</a>

    {{ include('attire_category/_delete_form.html.twig') }}
{% endblock %}